<?xml encoding="utf-8" ?>
<Window Name="window" AllowsTransparency1="True" Title="HQPlayer（Beta0.1.0）" ResizeMode="CanResize" WindowStyle="ThreeDBorderWindow" 
        CaptionHeight="20" Width="580" Height="400" MinWidth="380" MinHeight="300" >

    <Grid Margin="0" Name="RootGrid" >
        <!--标题栏-->
        <DockPanel VerticalAlignment="Top" Height="32">
            <StackPanel VerticalAlignment="Top" Height="32" Orientation="Horizontal" DockPanel.Dock="Right">
                <MinimizeButton ToolTip="最小化" Focusable="False" Width="25" Height="25" VerticalAlignment="Top" />
                <MaximizeButton ToolTip="最大化" Focusable="False" Width="25" Height="25" VerticalAlignment="Top" />
                <CloseButton ToolTip="关闭" Width="25" Height="25" VerticalAlignment="Top" />
            </StackPanel>
            <TextBlock Text="{Binding Title,RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}, AncestorLevel=1}}" FontSize="12" Margin="4" VerticalAlignment="Center"/>
            
        </DockPanel>

        <Grid Margin="0,32,0,0" >
            <Grid>
                <Grid Name="layPlayer" Margin="0,4,0,38" />
            </Grid>

            <DockPanel Height="38" VerticalAlignment="Bottom">
                <Grid Margin="2,4,2,2" DockPanel.Dock="Top">
                    <ProgressBar Name="playPB" Height="8" HorizontalAlignment="Stretch" Value="1" />
                </Grid>
                <Button Name="btnPlay" Content="播放" MinWidth="50" Margin="2" Click="OnClickButton" Padding="2"
                        VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Margin="2" VerticalAlignment="Center" >
                    
                    <TextBlock Name="curDuration" Text="00:00:00.000" />
                    <TextBlock Text="/" />
                    <TextBlock Name="duration" Text="00:00:00.000" />
                </StackPanel>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
